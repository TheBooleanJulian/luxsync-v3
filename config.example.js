// Local dev config — copy this file to config.js and fill in your own key.
// config.js is gitignored, so it never gets committed or pushed.
//
//   cp config.example.js config.js
//
// This only pre-fills the API key field on page load so you don't have to
// paste it in every time during local dev. It is NOT used in production —
// clients still enter their own key (see README "Known limitations").
window.LUXSYNC_API_KEY = "";
